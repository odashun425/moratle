<h1>履修・就活に役立つ大学授業の情報コミュニティ</h1>
  <div class="contents row" >
    <% @reviews.each do |review| %>
    <div class="content_post">
        <%= simple_format(review.review) %>
         <span class="name"><%= review.nickname %></span>
      </div>
    <% end %>
  </div>